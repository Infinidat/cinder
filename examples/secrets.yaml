parameter_defaults:
  ContainerImageRegistryCredentials:
    registry.redhat.io:
      # Replace with real ones to be able to login into registry.redhat.io registry
      # Required for the rest non-cinder images pull (like keystone, neutron, etc)
      <rh_creds_login>: 'your_rh_container_secret_to_access_registry.redhat.io'
